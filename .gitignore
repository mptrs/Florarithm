node_modules
dist
.DS_Store
test-results
playwright-report
tsconfig.tsbuildinfo

# A seeded design canvas is generated from the .dc.html artboards beside it;
# the artboards are the source, the multi-megabyte bundle with the editor baked
# into it is not.
design/florarithm-design-system.html
design/plant-redesign/plant-page.html
design/today-redesign/florarithm-today.html

# Impeccable's compiled per-platform engine — downloaded once per machine on
# first run, not something every clone should carry a darwin-arm64 binary for.
.claude/skills/impeccable/scripts/bin/
.github/skills/impeccable/scripts/bin/

# Per-session scan cache the design hook writes on every edit — local and
# disposable, not something a commit should carry.
.impeccable/hook.cache.json